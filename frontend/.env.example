# URL publica de la API. Vite la incrusta en el bundle al compilar, asi que
# tiene que estar definida en el entorno de build del hosting del frontend.
# En desarrollo se puede omitir: el cliente cae a http://127.0.0.1:8000.
VITE_API_URL=https://tu-api.example.com
