# Versiones fijadas: sin fijar, el contenedor se rearma distinto cada vez que se
# despliega y una version nueva de cualquiera de estas puede romperlo sin que
# haya cambiado una linea de codigo.
fastapi==0.141.1
uvicorn[standard]==0.52.4
google-cloud-bigquery==3.45.0
# Se declara explicitamente porque main.py importa google.oauth2.service_account
# para leer las credenciales desde una variable de entorno. Llegaba igual como
# dependencia de bigquery, pero lo que se importa se declara.
google-auth==2.57.0
python-dotenv==1.2.3
# FALTABA, y el contenedor arrancaba y se caia: interpretar_canasta.py la importa
# para la canasta con IA. En el venv local estaba instalada a mano, asi que en
# desarrollo no se notaba.
# DEUDA CONOCIDA: esta libreria esta discontinuada, hay que migrar a google-genai.
google-generativeai==0.8.6
# Zonas horarias para decir a que hora vuelve el sitio cuando se agota la cuota
# de BigQuery. zoneinfo usa la base del sistema operativo, y ni Windows ni la
# imagen python-slim la traen completa: sin este paquete, ZoneInfo falla.
tzdata==2026.3
